% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ststar.R
\name{ststar}
\alias{ststar}
\title{Generate a star polygon string art pattern}
\usage{
ststar(
  n = 5,
  k = 2,
  col = "blue",
  lwd = 1,
  plot = TRUE,
  show_points = TRUE,
  show_labels = FALSE,
  verbose = FALSE,
  radius = 1,
  rotate = pi/2,
  show_strings = TRUE,
  template = FALSE,
  draw_polygon = TRUE,
  border_col = "grey50",
  border_lwd = 1,
  point_col = "black",
  point_cex = 0.8,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.7,
  label_col = "black",
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs on the circle. Must be at least 3.}

\item{k}{Integer. Star step. Must satisfy \verb{1 <= k <= n - 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}.}

\item{lwd}{Positive number. Line width used to draw the star.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{radius}{Positive number. Circle radius.}

\item{rotate}{Numeric. Rotation angle in radians applied to the star.}

\item{show_strings}{Logical. If \code{TRUE}, draws the star connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
star connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{draw_polygon}{Logical. If \code{TRUE}, draws the underlying regular polygon.}

\item{border_col}{Polygon border color.}

\item{border_lwd}{Positive number. Border line width.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with columns \code{index}, \code{x}, and \code{y}.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, and \code{length}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{ststar()} generates a classical star polygon \code{{n/k}} by placing \code{n} pegs on
a circle and connecting each peg to the peg \code{k} positions ahead.
}
\details{
The star polygon uses the additive modular rule

\code{to = ((from + k - 1) \%\% n) + 1}.

The greatest common divisor \code{gcd(n, k)} determines the number of cycles. If
\code{gcd(n, k) = 1}, the star is a single cycle. Otherwise, the construction
decomposes into several independent cycles.
}
\examples{
ststar()
ststar(n = 5, k = 2)
ststar(n = 7, k = 2)
ststar(n = 8, k = 3)
ststar(template = TRUE)

}
