% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stnet.R
\name{stnet}
\alias{stnet}
\title{Generate a string art net from two rays}
\usage{
stnet(
  n = 30,
  k = 1,
  col = "blue",
  lwd = 1,
  plot = TRUE,
  show_points = TRUE,
  show_labels = FALSE,
  verbose = FALSE,
  length1 = 1,
  length2 = 1,
  angle = pi/2,
  rotate = 0,
  show_strings = TRUE,
  template = FALSE,
  show_envelope = FALSE,
  envelope_col = "red",
  envelope_lwd = 1,
  envelope_lty = 2,
  point_col = "black",
  point_cex = 0.8,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.7,
  label_col = "black",
  border_col = "grey50",
  border_lwd = 1,
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs placed on each ray. Must be at least 3.}

\item{k}{Integer. Number of shifted sweeps used in the construction. Must
satisfy \verb{1 <= k <= n - 1}. The classical net is obtained with \code{k = 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}.}

\item{lwd}{Positive number. Line width used to draw the strings.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{length1}{Positive number. Length of the first ray.}

\item{length2}{Positive number. Length of the second ray.}

\item{angle}{Numeric. Angle in radians from the first ray to the second ray.
Must not be a multiple of \code{pi}.}

\item{rotate}{Numeric. Rotation angle in radians applied to the whole net.}

\item{show_strings}{Logical. If \code{TRUE}, draws the string connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
string connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{show_envelope}{Logical. If \code{TRUE}, draws the theoretical envelope of
the basic construction.}

\item{envelope_col}{Color used for the theoretical envelope.}

\item{envelope_lwd}{Positive number. Line width used for the envelope.}

\item{envelope_lty}{Line type used for the envelope.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{border_col}{Ray color.}

\item{border_lwd}{Positive number. Ray line width.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with columns \code{index}, \code{x}, \code{y}, \code{ray}, and
\code{local_index}.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, \code{length}, \code{sweep},
\code{offset}, \code{local_from}, and \code{local_to}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{stnet()} generates a string art net by placing pegs on two rays that share
a common vertex and connecting corresponding peg positions. The construction
generalizes the classical parabolic string art envelope by allowing the angle
between the two supporting rays to vary.
}
\details{
The construction uses two rays with a common vertex. Pegs are placed uniformly
along the first ray from the common vertex to the first endpoint and along the
second ray from the second endpoint back to the common vertex.

For \code{k = 1}, the peg at local position \code{i} on the first ray is connected to
the peg at local position \code{i} on the second ray. In oblique coordinates
determined by the two rays, the theoretical envelope is given by

\verb{C(t) = t^2 A + (1 - t)^2 B},

where \code{A} and \code{B} are the endpoints of the two rays and \verb{0 <= t <= 1}.

For \code{k > 1}, the function adds shifted sweeps of the same construction,
producing denser string art nets.
}
\examples{
stnet()

res <- stnet(plot = FALSE)
head(res$pegs)
head(res$connections)
res$total_length

stnet(n = 40, k = 1, angle = pi / 2, col = "steelblue")
stnet(n = 40, k = 3, angle = 2 * pi / 3, col = "firebrick", lwd = 0.7)
stnet(show_envelope = TRUE)
stnet(template = TRUE)

}
