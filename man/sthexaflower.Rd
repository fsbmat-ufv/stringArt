% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sthexaflower.R
\name{sthexaflower}
\alias{sthexaflower}
\title{Generate a hexagonal flower string art pattern}
\usage{
sthexaflower(
  n = 24,
  k = 5,
  col = c("black", "forestgreen", "darkorange", "deepskyblue4", "firebrick", "purple"),
  lwd = 1,
  plot = TRUE,
  show_points = TRUE,
  show_labels = FALSE,
  verbose = FALSE,
  r = 1,
  scale_mid = 0.72,
  scale_inner = 0.42,
  offset_mid = 0,
  offset_inner = 0,
  rotate = 0,
  show_strings = TRUE,
  template = FALSE,
  point_col = "black",
  point_cex = 0.8,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.7,
  label_col = "black",
  border_col = "grey50",
  border_lwd = 1,
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs in each hexagonal circuit. Must be a
multiple of 6 and at least 6.}

\item{k}{Integer. Step used in the local modular connection rule. Must
satisfy \verb{1 <= k <= n - 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}. It may have length
1 or 6. If length 6, colors are used by sector.}

\item{lwd}{Positive number. Line width used to draw the strings.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{r}{Positive number. Radius of the outer hexagonal circuit.}

\item{scale_mid}{Positive number in \verb{(0, 1)}. Scale of the middle hexagonal
circuit relative to the outer circuit.}

\item{scale_inner}{Positive number in \verb{(0, scale_mid)}. Scale of the inner
hexagonal circuit relative to the outer circuit.}

\item{offset_mid}{Numeric value in \verb{[0, 1)}. Discrete relative offset applied
to the middle circuit along the peg sequence.}

\item{offset_inner}{Numeric value in \verb{[0, 1)}. Discrete relative offset
applied to the inner circuit along the peg sequence.}

\item{rotate}{Numeric. Rotation angle in radians applied to the whole figure.}

\item{show_strings}{Logical. If \code{TRUE}, draws the string connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
string connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{border_col}{Hexagonal border color.}

\item{border_lwd}{Positive number. Hexagonal border line width.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with peg coordinates and metadata.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, \code{length}, \code{block}, and
\code{sector}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{sthexaflower()} generates a string art pattern based on three concentric
hexagonal peg circuits and one central peg. The construction is fully
reproducible and returns both the peg coordinates and the connection table.
}
\details{
The function builds three concentric hexagonal circuits with \code{n} pegs each
and one central peg.

The peg table contains the columns \code{index}, \code{x}, \code{y}, \code{group}, \code{layer}, and
\code{local_index}.

The construction uses four connection blocks:
\itemize{
\item \code{outer_border}: consecutive connections on the outer hexagon.
\item \code{outer_to_middle}: connections from the outer circuit to the middle circuit.
\item \code{middle_to_inner}: connections from the middle circuit to the inner circuit.
\item \code{vertices_to_center}: connections from the outer vertices to the central peg.
}

The local additive modular rule used in the two radial blocks is

\code{to_local = ((from_local + k - 1) \%\% n) + 1}.
}
\examples{
sthexaflower()

res <- sthexaflower(plot = FALSE)
head(res$pegs)
head(res$connections)
res$total_length

sthexaflower(n = 30, k = 7, col = "steelblue", lwd = 0.8)
sthexaflower(n = 24, k = 5, show_points = TRUE, show_labels = TRUE)
sthexaflower(template = TRUE)

}
