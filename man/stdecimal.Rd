% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stdecimal.R
\name{stdecimal}
\alias{stdecimal}
\title{Generate a string art path from a rational decimal expansion}
\usage{
stdecimal(
  numerator = 1,
  denominator = 7,
  n = 10,
  k = 2,
  col = "blue",
  lwd = 1,
  plot = TRUE,
  show_points = TRUE,
  show_labels = TRUE,
  verbose = FALSE,
  radius = 1,
  rotate = pi/2,
  include_integer_part = TRUE,
  show_strings = TRUE,
  template = FALSE,
  point_col = "black",
  point_cex = 0.9,
  point_pch = 21,
  point_bg = "white",
  label_cex = 0.8,
  label_col = "black",
  border_col = "grey50",
  border_lwd = 1,
  bg = "white",
  main = NULL
)
}
\arguments{
\item{numerator}{Integer. Numerator of the rational number.}

\item{denominator}{Integer. Denominator of the rational number. Must be
nonzero.}

\item{n}{Integer. Numeral base and number of digit pegs. Default is 10.}

\item{k}{Integer. Number of repetitions of the repetend shown in the plot when
the expansion is repeating. Must be at least 1.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}.}

\item{lwd}{Positive number. Line width used to draw the digit path.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the digit pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws digit labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{radius}{Positive number. Circle radius.}

\item{rotate}{Numeric. Rotation angle in radians applied to the digit circle.}

\item{include_integer_part}{Logical. If \code{TRUE}, includes the integer part of
the rational number in the displayed digit sequence.}

\item{show_strings}{Logical. If \code{TRUE}, draws the digit connections.}

\item{template}{Logical. If \code{TRUE}, draws only the digit template, without
connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{border_col}{Border color of the digit circle.}

\item{border_lwd}{Positive number. Border line width.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with columns \code{index}, \code{x}, \code{y}, and \code{digit}.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, \code{length},
\code{digit_from}, \code{digit_to}, and \code{position}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{stdecimal()} places digit pegs on a circle and connects consecutive digits
in the expansion of a rational number. By default, the function uses base 10,
so the pegs are labeled from 0 to 9.
}
\details{
The function computes the base-\code{n} expansion of \code{numerator / denominator}
using exact long division. When the expansion is repeating, the repetend is
displayed \code{k} times after the preperiod.

For the default setting \code{n = 10}, the function is especially useful for
exploring decimal expansions, repeating decimals, periodicity, and patterns
in rational numbers.
}
\examples{
stdecimal()
stdecimal(1, 7)
stdecimal(1, 13)
stdecimal(22, 7)
stdecimal(template = TRUE)

}
