% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stpolygon.R
\name{stpolygon}
\alias{stpolygon}
\title{Generate string art on a regular polygon}
\usage{
stpolygon(
  n = 60,
  k = 7,
  col = "blue",
  lwd = 1,
  plot = TRUE,
  show_points = TRUE,
  show_labels = FALSE,
  verbose = FALSE,
  sides = 5,
  radius = 1,
  rotate = pi/2,
  show_strings = TRUE,
  template = FALSE,
  point_col = "black",
  point_cex = 0.8,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.7,
  label_col = "black",
  border_col = "grey50",
  border_lwd = 1,
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs placed along the polygon boundary. Must be
at least 3 and at least \code{sides}.}

\item{k}{Integer. Additive modular step used in the connection rule. Must
satisfy \verb{1 <= k <= n - 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}.}

\item{lwd}{Positive number. Line width used to draw the strings.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{sides}{Integer. Number of polygon sides. Must be at least 3.}

\item{radius}{Positive number. Circumradius of the polygon.}

\item{rotate}{Numeric. Rotation angle in radians applied to the polygon.}

\item{show_strings}{Logical. If \code{TRUE}, draws the string connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
string connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{border_col}{Polygon border color.}

\item{border_lwd}{Positive number. Polygon border line width.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with columns \code{index}, \code{x}, \code{y}, \code{side},
and \code{local_index}.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, and \code{length}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{stpolygon()} generates a string art figure on the boundary of a regular
polygon. Pegs are distributed uniformly along the polygon perimeter and
connected using an additive modular rule.
}
\details{
The polygon is inscribed in a circle of radius \code{radius}. Pegs are distributed
uniformly along the full boundary, not only at the vertices. The additive
modular connection rule is:

\code{to = ((from + k - 1) \%\% n) + 1}.

This construction supports the exploration of regular polygons, central and
interior angles, symmetry, congruence, divisibility, and modular arithmetic.
}
\examples{
stpolygon()
stpolygon(sides = 5)
stpolygon(sides = 6)
stpolygon(sides = 8)
stpolygon(n = 60, k = 7, sides = 5)
stpolygon(template = TRUE)

}
