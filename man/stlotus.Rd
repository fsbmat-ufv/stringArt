% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stlotus.R
\name{stlotus}
\alias{stlotus}
\title{Generate a lotus-like string art pattern}
\usage{
stlotus(
  n = 40,
  k = 11,
  col = "deeppink4",
  lwd = 0.8,
  plot = TRUE,
  show_points = FALSE,
  show_labels = FALSE,
  verbose = FALSE,
  petals = 5,
  outer_radius = 1,
  petal_radius = 0.34,
  petal_center_radius = 0.34,
  inner_radius = 0.18,
  rotate = 0,
  show_strings = TRUE,
  template = FALSE,
  point_col = "black",
  point_cex = 0.6,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.6,
  label_col = "black",
  border_col = "grey50",
  border_lwd = 1,
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs in each circular module. Must be at least 3.}

\item{k}{Integer. Additive modular step used in each module. Must satisfy
\verb{1 <= k <= n - 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}. It may have length
1 or \code{petals + 2}. If length is 1, the same color is used for all modules.}

\item{lwd}{Positive number. Line width used to draw the strings.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{petals}{Integer. Number of petal modules around the center.}

\item{outer_radius}{Positive number. Radius of the outer circular module.}

\item{petal_radius}{Positive number. Radius of each petal module.}

\item{petal_center_radius}{Positive number. Distance from the origin to each
petal center.}

\item{inner_radius}{Positive number. Radius of the central circular module.}

\item{rotate}{Numeric. Rotation angle in radians applied to the whole figure.}

\item{show_strings}{Logical. If \code{TRUE}, draws the string connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
string connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{border_col}{Border color used for the module outlines.}

\item{border_lwd}{Positive number. Border line width.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result} with:
\describe{
\item{pegs}{A \code{data.frame} with peg coordinates and metadata.}
\item{connections}{A \code{data.frame} with columns \code{connection_index},
\code{from}, \code{to}, \code{x_from}, \code{y_from}, \code{x_to}, \code{y_to}, \code{length}, \code{module},
\code{local_from}, and \code{local_to}.}
\item{total_length}{Total string length.}
\item{audit}{A character vector with audit information.}
\item{meta}{A list with construction metadata.}
}
}
\description{
\code{stlotus()} generates a stylized lotus-like string art figure by combining
one outer circular module, one central circular module, and several petal
modules arranged around the center.
}
\details{
The figure is built from \code{petals + 2} circular modules:
\itemize{
\item one outer circular module;
\item \code{petals} petal modules;
\item one central circular module.
}

Each module contains \code{n} equally spaced pegs and uses the same additive
modular rule:

\code{to_local = ((from_local + k - 1) \%\% n) + 1}.

The final figure is obtained by superimposing all module connections.
}
\examples{
stlotus()

res <- stlotus(plot = FALSE)
head(res$pegs)
head(res$connections)
res$total_length

stlotus(n = 50, k = 13, col = "deeppink4", lwd = 0.8)
stlotus(show_points = TRUE, show_labels = TRUE)
stlotus(template = TRUE)

}
