% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strose.R
\name{strose}
\alias{strose}
\title{Generate a rose-like string art pattern}
\usage{
strose(
  n = 240,
  k = 17,
  col = "deeppink4",
  lwd = 0.7,
  plot = TRUE,
  show_points = FALSE,
  show_labels = FALSE,
  verbose = FALSE,
  petals = 6,
  amplitude = 1,
  rotate = 0,
  show_strings = TRUE,
  template = FALSE,
  draw_curve = TRUE,
  border_col = "grey50",
  border_lwd = 1,
  point_col = "black",
  point_cex = 0.5,
  point_pch = 19,
  point_bg = "white",
  label_cex = 0.6,
  label_col = "black",
  bg = "white",
  main = NULL
)
}
\arguments{
\item{n}{Integer. Number of pegs sampled along the curve. Must be at least 3.}

\item{k}{Integer. Additive modular step used in the connection rule. Must
satisfy \verb{1 <= k <= n - 1}.}

\item{col}{String color passed to \code{\link[graphics:segments]{graphics::segments()}}.}

\item{lwd}{Positive number. Line width used to draw the strings.}

\item{plot}{Logical. If \code{TRUE}, draws the figure.}

\item{show_points}{Logical. If \code{TRUE}, draws the pegs.}

\item{show_labels}{Logical. If \code{TRUE}, draws peg labels.}

\item{verbose}{Logical. If \code{TRUE}, prints a short audit to the console.}

\item{petals}{Integer. Number of petals in the rose-like curve.}

\item{amplitude}{Positive number. Maximum radial amplitude.}

\item{rotate}{Numeric. Rotation angle in radians.}

\item{show_strings}{Logical. If \code{TRUE}, draws the string connections.}

\item{template}{Logical. If \code{TRUE}, draws only the peg template, without
string connections. This is equivalent to setting \code{show_strings = FALSE}
and \code{show_points = TRUE}.}

\item{draw_curve}{Logical. If \code{TRUE}, draws the underlying rose-like curve.}

\item{border_col}{Curve color.}

\item{border_lwd}{Positive number. Curve line width.}

\item{point_col}{Peg color.}

\item{point_cex}{Positive number. Peg size.}

\item{point_pch}{Plotting symbol used for pegs.}

\item{point_bg}{Peg background color when applicable.}

\item{label_cex}{Positive number. Label size.}

\item{label_col}{Label color.}

\item{bg}{Plot background color.}

\item{main}{Optional plot title. If \code{NULL}, no title is displayed.}
}
\value{
Invisibly returns a list of class \code{stringart_result}.
}
\description{
\code{strose()} generates a rose-like string art figure based on a polar curve of
the form \code{r(theta) = amplitude * (1 + cos(petals * theta)) / 2}.
}
\details{
The pegs are sampled from the polar curve
\code{r(theta) = amplitude * (1 + cos(petals * theta)) / 2}, which produces a
rose-like pattern with \code{petals} visible petals.

The connections follow the additive modular rule
\code{to = ((from + k - 1) \%\% n) + 1}.
}
\examples{
strose()
strose(petals = 6)
strose(petals = 8)
strose(template = TRUE)

}
